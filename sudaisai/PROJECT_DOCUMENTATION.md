# 📚 Comprehensive Codebase Documentation (`sudaisai`)

Welcome to the complete, file-by-file documentation for the **sudaisai** project. This document provides a granular explanation of every single file in the repository, including why it exists, what it contains, its exact function/purpose, and how it interacts within the overall Next.js application architecture.

---

## 📌 1. Root Configuration & Build System

### 1.1 [package.json](file:///e:/project/sudaisai/package.json)

- 🎯 **Why it Exists:** Serves as the central manifest for Node.js projects. It declares project dependencies, scripts, versioning, and environment dependencies.
- 📄 **What it Contains:** Project metadata (`name: "sudaisai"`), npm scripts (`dev`, `build`, `start`, `lint`), production dependencies (`@prisma/client`, `bcrypt`, `cors`, `dotenv`, `jsonwebtoken`, `mailersend`, `next`, `react`, `react-dom`), and dev dependencies (`tailwindcss`, `typescript`, `prisma`, `eslint`, `@types/*`).
- ⚙️ **Function & Purpose:** Defines the project requirements and entry points for package managers (npm/yarn/pnpm).
- 🔄 **How it Works:** When running `npm install`, npm resolves packages listed here. When running `npm run dev`, it executes `next dev` to start the Next.js local development server.

---

### 1.2 [package-lock.json](file:///e:/project/sudaisai/package-lock.json)

- 🎯 **Why it Exists:** Ensures deterministic builds across different environments by locking exact versions of all installed packages and their sub-dependencies.
- 📄 **What it Contains:** An autogenerated tree of all installed Node modules, their exact installed versions, integrity hashes, and dependency resolution graphs.
- ⚙️ **Function & Purpose:** Prevents "works on my machine" bugs caused by automatic minor/patch version updates in third-party libraries.
- 🔄 **How it Works:** Automatically generated and maintained by `npm`. Read by npm during `npm install` to enforce exact version matching.

---

### 1.3 [next.config.ts](file:///e:/project/sudaisai/next.config.ts)

- 🎯 **Why it Exists:** Contains custom configuration options for the Next.js framework build process and runtime environment.
- 📄 **What it Contains:** Exports a `nextConfig` object typed with `NextConfig` from `next`.
- ⚙️ **Function & Purpose:** Used to customize Next.js behaviors such as headers, redirects, image domains, output mode, and experimental flags.
- 🔄 **How it Works:** Loaded by Next.js on server startup (`next dev` or `next build`) to apply build-level configurations.

---

### 1.4 [tsconfig.json](file:///e:/project/sudaisai/tsconfig.json)

- 🎯 **Why it Exists:** Configures the TypeScript compiler (`tsc`) settings for type checking and transpilation.
- 📄 **What it Contains:** Compiler options including target JS version, JSX settings (`preserve`), module resolution strategy (`bundler`), strict mode settings, Next.js plugins, and path aliases (e.g. `@/*` mapping to `./src/*`).
- ⚙️ **Function & Purpose:** Enforces type safety across all `.ts` and `.tsx` files and enables module aliasing (`import prisma from '@/utils/prisma'`).
- 🔄 **How it Works:** Read by the TypeScript language server in IDEs and Next.js compiler during development and build verification.

---

### 1.5 [postcss.config.mjs](file:///e:/project/sudaisai/postcss.config.mjs)

- 🎯 **Why it Exists:** Configures PostCSS, a tool for transforming CSS styles with JavaScript plugins.
- 📄 **What it Contains:** Plugin exports registering `@tailwindcss/postcss` v4 plugin.
- ⚙️ **Function & Purpose:** Enables Tailwind CSS v4 compilation within Next.js CSS processing pipeline.
- 🔄 **How it Works:** Next.js uses PostCSS to process `@import "tailwindcss"` in `src/app/globals.css` into utility classes during styling builds.

---

### 1.6 [eslint.config.mjs](file:///e:/project/sudaisai/eslint.config.mjs)

- 🎯 **Why it Exists:** Configures ESLint for static code analysis and linting.
- 📄 **What it Contains:** Extends Next.js core Web Vitals linting rules (`next/core-web-vitals` and `next/typescript`).
- ⚙️ **Function & Purpose:** Automatically detects code code smells, syntax issues, unhandled promises, and React best practice violations.
- 🔄 **How it Works:** Run during `npm run lint` or automatically by IDE extensions on code save.

---

### 1.7 [.env](file:///e:/project/sudaisai/.env) & [.env.example](file:///e:/project/sudaisai/.env.example)

- 🎯 **Why it Exists:** Stores sensitive environment variables (like database credentials, secret keys) outside the source code repository.
- 📄 **What it Contains:** Key-value pairs such as `DATABASE_URL`, `DB_HOST`, `DB_PORT`, `DB_USER`, `DB_PASSWORD`, `DB_NAME`, and `MAILERSEND_API_KEY`. `.env.example` provides a sanitized template for developers.
- ⚙️ **Function & Purpose:** Provides secret management and environment-specific runtime configurations.
- 🔄 **How it Works:** Loaded by Next.js and Prisma CLI (`dotenv/config`) into `process.env`.

---

### 1.8 [.gitignore](file:///e:/project/sudaisai/.gitignore)

- 🎯 **Why it Exists:** Specifies intentional untracked files that Git should ignore.
- 📄 **What it Contains:** File patterns for `node_modules/`, `.next/`, `.env`, build logs, and OS system files.
- ⚙️ **Function & Purpose:** Prevents sensitive secrets, large dependencies, and generated build artifacts from being committed to source control.
- 🔄 **How it Works:** Read natively by Git before staging or committing changes.

---

### 1.9 [next-env.d.ts](file:///e:/project/sudaisai/next-env.d.ts)

- 🎯 **Why it Exists:** Global TypeScript declaration file maintained by Next.js.
- 📄 **What it Contains:** TypeScript triple-slash references to Next.js image and asset types (`/// <reference types="next" />`).
- ⚙️ **Function & Purpose:** Ensures TypeScript recognizes static asset imports (PNGs, SVGs, JPGs) and Next.js ambient environment variables.
- 🔄 **How it Works:** Automatically included by TypeScript compiler during project type evaluation.

---

### 1.10 [tsconfig.tsbuildinfo](file:///e:/project/sudaisai/tsconfig.tsbuildinfo)

- 🎯 **Why it Exists:** Generated build metadata file created when incremental compilation (`"incremental": true` or `"composite": true`) is enabled in `tsconfig.json`.
- 📄 **What it Contains:** Internal compiler build state, project graph, declaration file hashes, and type check caches.
- ⚙️ **Function & Purpose:** Speeds up subsequent TypeScript compilation cycles (`tsc` or `next build`) by reusing prior build evaluations.
- 🔄 **How it Works:** Automatically read and updated by the TypeScript compiler on every build execution.

---

### 1.11 [.vscode/settings.json](file:///e:/project/sudaisai/.vscode/settings.json)

- 🎯 **Why it Exists:** Workspace-specific VS Code configuration file.
- 📄 **What it Contains:** `editor.tokenColorCustomizations` defining teal foreground styling (`#2DD4BF`) and italic font styles for code comments across TypeScript/JavaScript files.
- ⚙️ **Function & Purpose:** Standardizes comment syntax highlighting across developer IDE environments for enhanced code readability.
- 🔄 **How it Works:** Automatically applied by VS Code when opening the workspace directory.

---

## 📌 2. AI & Agent Guidelines

### 2.1 [AGENTS.md](file:///e:/project/sudaisai/AGENTS.md)

- 🎯 **Why it Exists:** Provides instructions and behavioral rules for AI coding assistants working in this repository.
- 📄 **What it Contains:** Next.js version rules and project-specific workflow rules (such as mandatory documentation rules).
- ⚙️ **Function & Purpose:** Aligns AI agents on repository conventions and breaking changes in Next.js versions.
- 🔄 **How it Works:** Automatically loaded by AI development tools (like Antigravity / Gemini / Cursor) on context start.

---

### 2.2 [.agents/AGENTS.md](file:///e:/project/sudaisai/.agents/AGENTS.md)

- 🎯 **Why it Exists:** Workspace-scoped override file for agent rules.
- 📄 **What it Contains:** Directives requiring updating documentation files whenever code files are added or modified.
- ⚙️ **Function & Purpose:** Enforces persistent rule compliance across agent sessions.
- 🔄 **How it Works:** Parsed by the agent framework's customization engine.

---

### 2.3 [CLAUDE.md](file:///e:/project/sudaisai/CLAUDE.md)

- 🎯 **Why it Exists:** Quick instruction file for Anthropic Claude / Claude Code CLI interactions.
- 📄 **What it Contains:** High-level project context pointers.
- ⚙️ **Function & Purpose:** Guides Claude models when operating in CLI mode.
- 🔄 **How it Works:** Automatically read by Claude CLI upon startup in workspace.

---

### 2.4 [skills-lock.json](file:///e:/project/sudaisai/skills-lock.json)

- 🎯 **Why it Exists:** Locks installed AI skill definitions and versions.
- 📄 **What it Contains:** Metadata on installed agent tool packages and skills.
- ⚙️ **Function & Purpose:** Ensures consistent agent capability tools across environments.
- 🔄 **How it Works:** Read by the agent framework to register custom skills.

---

### 2.5 [README.md](file:///e:/project/sudaisai/README.md)

- 🎯 **Why it Exists:** Primary documentation landing file for developers opening the project.
- 📄 **What it Contains:** Getting started instructions, build scripts, stack details, and Next.js links.
- ⚙️ **Function & Purpose:** Onboards developers to run and test the application.
- 🔄 **How it Works:** Displayed on GitHub project homepage.

---

### 2.6 [to-know.md](file:///e:/project/sudaisai/to-know.md)

- 🎯 **Why it Exists:** Project security reference guide and technical knowledge repository.
- 📄 **What it Contains:** 15 cyber security attack vectors and their explicit mitigation strategy matrix (SQL Injection, Bcrypt DoS, Rate Limiting, Timing Attack dummy hashes, ReDoS protection via RE2JS, Null Byte Stripping, Type Confusion guards), along with reference concepts on NextRequest/NextResponse, WebFetch API, Edge Runtime, and TCP/SSL networking protocols.
- ⚙️ **Function & Purpose:** Serves as the authoritative architectural security compliance checklist and knowledge base for developers working on authentication and API routes.
- 🔄 **How it Works:** Referenced during security audits, code reviews, and API endpoint implementation.

---

### 2.7 Agent Skills Packages (`.agents/skills/`, `.claude/skills/`, `.windsurf/skills/`)

- 🎯 **Why it Exists:** Custom AI agent skill packages for database operations and agent framework integration.
- 📄 **What it Contains:** 9 modular Prisma skill packages (`prisma-cli`, `prisma-client-api`, `prisma-compute`, `prisma-database-setup`, `prisma-driver-adapter-implementation`, `prisma-mongodb-upgrade`, `prisma-postgres`, `prisma-postgres-setup`, `prisma-upgrade-v7`) and editor tool link definitions.
- ⚙️ **Function & Purpose:** Extends AI coding assistants (Antigravity, Claude CLI, Windsurf) with specialized domain knowledge for Prisma v7 migrations, database setup, and driver adapter workflows.
- 🔄 **How it Works:** Loaded dynamically by AI agents when performing database schema modifications, driver setups, or framework upgrades.

---

## 📌 3. Database & Database Client Layer (`prisma/`)

### 3.1 [prisma/schema.prisma](file:///e:/project/sudaisai/prisma/schema.prisma)

- 🎯 **Why it Exists:** The single source of truth for the project's database schema.
- 📄 **What it Contains:**
  - `generator client`: Configures JS client generator.
  - `datasource db`: Specifies database dialect (`postgresql`). (Note: In Prisma v7+, database URL is declared in `prisma.config.ts`).
  - `enum Role`: User access permission roles (`USER`, `ADMIN`).
  - `model PendingRegistration`: Temporary table for unverified 2-Factor/OTP registrations storing `email`, `username`, `passwordHash`, `otpHash`, `otpExpiresAt`, `attempts`, and indexed by `otpExpiresAt`.
  - `model User`: Persistent user account model mapped to `users` SQL table storing CUID `id`, `email`, `username`, `passwordHash`, `isEmailVerified`, `role`, `createdAt`, and `updatedAt`.
- ⚙️ **Function & Purpose:** Generates type-safe database access code (`PrismaClient`) and handles database schema migrations.
- 🔄 **How it Works:** Executed by `npx prisma generate` to populate `@prisma/client` and `npx prisma validate` to verify schema integrity.

---

### 3.2 [prisma.config.ts](file:///e:/project/sudaisai/prisma.config.ts)

- 🎯 **Why it Exists:** Custom configuration file for Prisma CLI tools (required in Prisma v7+).
- 📄 **What it Contains:** Configuration mapping schema path (`prisma/schema.prisma`), migrations path (`prisma/migrations`), and database URL resolution (`process.env["DATABASE_URL"]`).
- ⚙️ **Function & Purpose:** Provides connection credentials and configuration settings for Prisma CLI commands (migrate, validate, generate).
- 🔄 **How it Works:** Read by Prisma CLI during schema compilation, validation, and migration execution.

---

### 3.3 [prisma/prisma.ts](file:///e:/project/sudaisai/prisma/prisma.ts)

- 🎯 **Why it Exists:** Implements a global singleton pattern for the `PrismaClient` using `PrismaMariaDb` driver adapter required in Prisma 7+.
- 📄 **What it Contains:** Instantiates `PrismaMariaDb` adapter and attaches `PrismaClient` to `global.prisma`. Configuration uses variables from `process.env` (e.g., `DB_HOST`, `DB_PORT`).
- ⚙️ **Function & Purpose:** Prevents database connection pool exhaustion in Next.js development mode during Hot Module Reloading (HMR) while enforcing Prisma 7 driver adapter compliance.
- 🔄 **How it Works:** Reuses the existing client instance in development across file saves.

---

### 3.4 [src/utils/prisma.ts](file:///e:/project/sudaisai/src/utils/prisma.ts)

- 🎯 **Why it Exists:** Application-level export for the Prisma database client instance initialized with `PrismaMariaDb` driver adapter.
- 📄 **What it Contains:** Singleton `createPrismaClient()` helper instantiating `PrismaClient({ adapter: new PrismaMariaDb(...) })`.
- ⚙️ **Function & Purpose:** Provides type-safe, alias-compatible database access (`import prisma from '@/utils/prisma'`) for Next.js App Router API routes and server actions.
- 🔄 **How it Works:** Imported directly by API endpoints (such as `src/app/api/auth/signup/route.ts` and `verify-otp/route.ts`) to execute database queries.

---

### 3.5 [src/utils/email.ts](file:///e:/project/sudaisai/src/utils/email.ts)

- 🎯 **Why it Exists:** Provides email dispatch utility functions using Nodemailer and Gmail for sending OTP verification codes.
- 📄 **What it Contains:** `sendOTPEmail(toEmail, otp)` function authenticating with a provided Gmail account and App Password, generating HTML verification email templates.
- ⚙️ **Function & Purpose:** Sends 6-digit OTP codes for account verification using Gmail's SMTP infrastructure.
- 🔄 **How it Works:** Called by `POST /api/auth/signup` to dispatch verification codes to users during registration.

---

## 📌 4. Next.js App Router Core (`src/app/`)

### 4.1 [src/app/layout.tsx](file:///e:/project/sudaisai/src/app/layout.tsx)

- 🎯 **Why it Exists:** The root layout component shared across all pages in the Next.js App Router.
- 📄 **What it Contains:** Google Fonts instantiation (`Geist`, `Geist_Mono`), global metadata (title: `sudaisai - Next-Gen AI Conversational Engine`, description, logo icon), and HTML root structure.
- ⚙️ **Function & Purpose:** Establishes document structure (`<html>`, `<body>`), font variables, global CSS loading, and SEO metadata.
- 🔄 **How it Works:** Wraps all page components (`children`) on every request rendered by Next.js.

---

### 4.2 [src/app/page.tsx](file:///e:/project/sudaisai/src/app/page.tsx)

- 🎯 **Why it Exists:** The root page component for the application homepage (`/`).
- 📄 **What it Contains:** Renders `<LandingPage />` component.
- ⚙️ **Function & Purpose:** Acts as the entry point view for incoming site visitors.
- 🔄 **How it Works:** Next.js routes incoming requests to `/` directly to this file, displaying the landing page.

---

### 4.3 [src/app/globals.css](file:///e:/project/sudaisai/src/app/globals.css)

- 🎯 **Why it Exists:** Root stylesheet containing global CSS styles, Tailwind directives, and CSS custom variables.
- 📄 **What it Contains:** `@import "tailwindcss"`, `:root` color tokens (`--background`, `--foreground`), dark mode media overrides, and base `body` styles.
- ⚙️ **Function & Purpose:** Applies styling themes and resets across the entire app.
- 🔄 **How it Works:** Imported in `layout.tsx` so styles apply globally across all components.

---

### 4.4 [src/app/api/auth/signup/route.ts](file:///e:/project/sudaisai/src/app/api/auth/signup/route.ts)

- 🎯 **Why it Exists:** Backend HTTP POST API route for initiating 2-step user signup and dispatching OTP code.
- 📄 **What it Contains:**
  1. Input validation & sanitization (email format, username constraints, password strength).
  2. Duplicate user check using Prisma (`prisma.user.findFirst`).
  3. Secure cryptographic 6-digit OTP generation (`crypto.randomInt`).
  4. Password & OTP hashing (`bcrypt.hash`).
  5. Pending registration record insertion/update in `PendingRegistration` model.
  6. Email dispatch via `sendOTPEmail`.
- ⚙️ **Function & Purpose:** Initiates user registration flow by creating a pending registration record and emailing a 6-digit OTP.
- 🔄 **How it Works:** Accessible via `POST /api/auth/signup`. Returns 200 JSON asking the user to submit their OTP code.

---

### 4.5 [src/app/api/auth/verify-otp/route.ts](file:///e:/project/sudaisai/src/app/api/auth/verify-otp/route.ts)

- 🎯 **Why it Exists:** Backend HTTP POST API route for verifying OTP codes and completing user account registration.
- 📄 **What it Contains:**
  1. Input validation (6-digit numeric OTP format check).
  2. Rate limiting check (max 5 verification attempts per 15 minutes per IP).
  3. Pending registration lookup in `PendingRegistration` model.
  4. Expiration check (10-minute TTL window) and failed attempt counter enforcement (max 5 attempts).
  5. Cryptographic hash comparison (`bcrypt.compare`).
  6. Atomic Prisma transaction (`prisma.$transaction`) creating `User` record and deleting `PendingRegistration`.
- ⚙️ **Function & Purpose:** Guarantees that users are only registered after presenting a valid, non-expired OTP code.
- 🔄 **How it Works:** Accessible via `POST /api/auth/verify-otp`. Returns 201 Created JSON on success.

---

### 4.6 [src/proxy.ts](file:///e:/project/sudaisai/src/proxy.ts)

- 🎯 **Why it Exists:** Intercepts incoming HTTP requests at the edge/server level before reaching API route handlers.
- 📄 **What it Contains:** `proxy(request: NextRequest)` export handler and `config.matcher` targeting `/api/auth/:path*`.
- ⚙️ **Function & Purpose:** Serves as the central edge/server request interceptor.
- 🔄 **How it Works:** Executed automatically by Next.js request pipeline on matched routes before delegating to route handlers.

---

## 📌 5. UI Components Layer (`src/components/landing-page/`)

### 5.1 [src/components/landing-page/LandingPage.tsx](file:///e:/project/sudaisai/src/components/landing-page/LandingPage.tsx)

- 🎯 **Why it Exists:** Wrapper component assembling all individual section components of the landing page.
- 📄 **What it Contains:** Integrates `<Navbar />`, `<HeroSection />`, `<FeaturesSection />`, `<ProcessSection />`, `<TestimonialsSection />`, and `<Footer />`.
- ⚙️ **Function & Purpose:** Maintains layout flow and background theme for the landing experience.
- 🔄 **How it Works:** Imported and rendered inside `src/app/page.tsx`.

---

### 5.2 [src/components/landing-page/HeroSection.tsx](file:///e:/project/sudaisai/src/components/landing-page/HeroSection.tsx)

- 🎯 **Why it Exists:** The primary headline section of the landing page designed to capture user interest.
- 📄 **What it Contains:** Badges, main headline, call-to-action buttons ("Start Free Trial", "Play Video"), social proof rating, and an interactive video preview card running `/hero-video.mp4`.
- ⚙️ **Function & Purpose:** Communicates the core value proposition of the AI engine and provides interactive video playback controls.
- 🔄 **How it Works:** Client component ('use client') utilizing `useVideoPlayer` hook to control video play/pause and track time updates.

---

### 5.3 [src/components/landing-page/Navbar.tsx](file:///e:/project/sudaisai/src/components/landing-page/Navbar.tsx)

- 🎯 **Why it Exists:** Top navigation bar component.
- 📄 **What it Contains:** Brand logo (`/logo.png`), navigation links (Features, Process, Testimonials), and Auth CTA buttons ("Sign In", "Get Started").
- ⚙️ **Function & Purpose:** Provides sticky site-wide navigation and quick access to sign-in functionality.
- 🔄 **How it Works:** Positioned at the top of `LandingPage.tsx`.

---

### 5.4 [src/components/landing-page/FeaturesSection.tsx](file:///e:/project/sudaisai/src/components/landing-page/FeaturesSection.tsx)

- 🎯 **Why it Exists:** Grid container highlighting key technical capabilities of the product.
- 📄 **What it Contains:** Heading texts and maps over feature items to render individual `<FeatureCard />` components.
- ⚙️ **Function & Purpose:** Showcases platform benefits (Zero Latency, Emotional AI, Dynamic Visuals).
- 🔄 **How it Works:** Rendered inside `LandingPage.tsx`.

---

### 5.5 [src/components/landing-page/FeatureCard.tsx](file:///e:/project/sudaisai/src/components/landing-page/FeatureCard.tsx)

- 🎯 **Why it Exists:** Reusable card component for displaying a single product feature.
- 📄 **What it Contains:** Icon container, title, description, and hover animation styles.
- ⚙️ **Function & Purpose:** Standardizes feature presentation.
- 🔄 **How it Works:** Instantiated repeatedly inside `FeaturesSection.tsx`.

---

### 5.6 [src/components/landing-page/ProcessSection.tsx](file:///e:/project/sudaisai/src/components/landing-page/ProcessSection.tsx)

- 🎯 **Why it Exists:** Illustrates the step-by-step workflow of how the AI companion works.
- 📄 **What it Contains:** Step numbers (01, 02, 03), step titles, descriptions, and visual connector styling.
- ⚙️ **Function & Purpose:** Guides users through the onboard and interaction process.
- 🔄 **How it Works:** Rendered sequentially inside `LandingPage.tsx`.

---

### 5.7 [src/components/landing-page/TestimonialsSection.tsx](file:///e:/project/sudaisai/src/components/landing-page/TestimonialsSection.tsx)

- 🎯 **Why it Exists:** Displays customer reviews and social proof.
- 📄 **What it Contains:** Grid mapping over reviewer data to render `<TestimonialCard />` components.
- ⚙️ **Function & Purpose:** Builds trust and credibility for visitors.
- 🔄 **How it Works:** Rendered inside `LandingPage.tsx`.

---

### 5.8 [src/components/landing-page/TestimonialCard.tsx](file:///e:/project/sudaisai/src/components/landing-page/TestimonialCard.tsx)

- 🎯 **Why it Exists:** Reusable card displaying an individual user review.
- 📄 **What it Contains:** Quote text, author name, avatar, role, and star ratings.
- ⚙️ **Function & Purpose:** Standardizes customer review presentation.
- 🔄 **How it Works:** Instantiated inside `TestimonialsSection.tsx`.

---

### 5.9 [src/components/landing-page/Footer.tsx](file:///e:/project/sudaisai/src/components/landing-page/Footer.tsx)

- 🎯 **Why it Exists:** Bottom footer area of the site.
- 📄 **What it Contains:** Brand summary, quick links, newsletter subscription input, copyright details, and social links.
- ⚙️ **Function & Purpose:** Provides supplementary navigation and legal/copyright information.
- 🔄 **How it Works:** Rendered at the bottom of `LandingPage.tsx`.

---

### 5.10 [src/components/landing-page/Button.tsx](file:///e:/project/sudaisai/src/components/landing-page/Button.tsx)

- 🎯 **Why it Exists:** Universal UI button component.
- 📄 **What it Contains:** Button variant styles (primary, secondary, outline) with hover and active states.
- ⚙️ **Function & Purpose:** Provides consistent button styling and behavior across sections.
- 🔄 **How it Works:** Imported by landing page components needing interactive button elements.

---

## 📌 6. Utilities & Hooks (`src/utils/`)

### 6.1 [src/utils/constants.tsx](file:///e:/project/sudaisai/src/utils/constants.tsx)

- 🎯 **Why it Exists:** Houses reusable custom React hooks and helper state logic.
- 📄 **What it Contains:** The `useVideoPlayer()` custom hook managing video HTML5 element state (`isPlaying`, `currentTime`, `duration`, `progressPercent`, `togglePlay`, `handleTimeUpdate`).
- ⚙️ **Function & Purpose:** Encapsulates media player state logic away from UI components.
- 🔄 **How it Works:** Imported by `HeroSection.tsx` to drive the video showcase card.

---

### 6.2 [src/utils/validation.ts](file:///e:/project/sudaisai/src/utils/validation.ts)

- 🎯 **Why it Exists:** Centralizes input validation rules, sanitization utilities, constant size limits, and security helper functions across the application. Uses `re2js` linear-time regex engine for ReDoS protection.
- 📄 **Exports & Function Specifications:**
  - **`INPUT_LIMITS`** _(Constant object)_: Single source of truth defining strict min/max size limits for inputs (`EMAIL_MIN_LENGTH`: 5, `EMAIL_MAX_LENGTH`: 254, `USERNAME_MIN_LENGTH`: 3, `USERNAME_MAX_LENGTH`: 30, `PASSWORD_MIN_LENGTH`: 10, `PASSWORD_MAX_LENGTH`: 128, `REQUEST_BODY_MAX_BYTES`: 4096).
  - **`isValidEmail(email: unknown): email is string`**: Type-guard function checking email format. Enforces RFC 5321 length limits, linear-time `RE2JS` regex matching (`emailLinearRegex`), and structural constraints (local <= 64, domain <= 253, no consecutive or leading/trailing dots).
  - **`isValidPassword(password: unknown): password is string`**: Type-guard wrapper that delegates to `validatePasswordStrength` and returns a simple `boolean`.
  - **`validatePasswordStrength(password: unknown): PasswordValidationResult`**: Evaluates password against security rules and returns `{ valid: boolean, errors: string[] }`. Enforces max length FIRST (<= 128 chars) to mitigate bcrypt Denial of Service (DoS), min length (>= 10 chars), lowercase, uppercase, numeric digit, special character via `RE2JS` patterns, and blocks single-character repetition (e.g. `"aaaaaaaaaa"`) using an $O(n)$ string loop (replacing non-regular regex backreferences).
  - **`isValidUsername(username: unknown): username is string`**: Type-guard validating username formatting. Requires 3-30 characters, linear-time `RE2JS` regex validation (`usernameLinearRegex`), forbids leading/trailing hyphens or underscores (`usernameEdgeSpecialRegex`), and explicitly rejects null bytes (`\0`).
  - **`sanitizeString(input: string): string`**: Sanitizes string inputs by stripping null bytes (`\0`), ASCII control characters (`[\x00-\x08\x0B\x0C\x0E-\x1F\x7F]`), and whitespace trimming.
  - **`safeCompare(a: string, b: string): boolean`**: Performs a constant-time bitwise string comparison to prevent timing side-channel attacks when checking secret tokens, hashes, or credentials.
- ⚙️ **Function & Purpose:** Ensures strict zero-trust input validation, sanitization against XSS/injection, timing attack resistance, ReDoS protection via `re2js` linear-time evaluation, and bcrypt DoS protection across all API routes and client input fields.
- 🔄 **How it Works:** Imported by server API routes (such as `src/app/api/auth/signup/route.ts`) and frontend interactive forms to validate and sanitize user inputs before processing or database mutations.

---

## 📌 7. Static Public Assets (`public/`)

### 7.1 [public/hero-video.mp4](file:///e:/project/sudaisai/public/hero-video.mp4)

- 🎯 **Why it Exists:** Demonstration video asset showcased on the homepage.
- 📄 **What it Contains:** MP4 video file demonstrating AI conversational interaction.
- ⚙️ **Function & Purpose:** Visually demonstrates the product's real-time capabilities to visitors.
- 🔄 **How it Works:** Served statically by Next.js at `/hero-video.mp4` and played inside `HeroSection.tsx`.

---

### 7.2 [public/logo.png](file:///e:/project/sudaisai/public/logo.png)

- 🎯 **Why it Exists:** Brand logo image asset.
- 📄 **What it Contains:** PNG graphics file of the `sudaisai` logo.
- ⚙️ **Function & Purpose:** Used for website branding, header logo, and favicon / Apple touch icon.
- 🔄 **How it Works:** Referenced in `layout.tsx` metadata and rendered inside `Navbar.tsx`.

---

## 📌 Updates (OTP & JWT Implementation)

### [src/utils/api-helpers.ts](file:///e:/project/sudaisai/src/utils/api-helpers.ts)
- 🎯 **Why it Exists:** Provides shared DRY functions for both signup and verify-otp routes.
- 📄 **What it Contains:** Functions for standardized JSON responses and safe JSON body parsing.
- ⚙️ **Function & Purpose:** Prevents code duplication and centralizes error handling and payload limit checks.

### [src/utils/jwt.ts](file:///e:/project/sudaisai/src/utils/jwt.ts)
- 🎯 **Why it Exists:** Handles the creation and validation of JSON Web Tokens for the OTP flow.
- 📄 **What it Contains:** createOTPToken, erifyOTPToken, and bearer token extraction functions.
- ⚙️ **Function & Purpose:** Acts as cryptographic proof that a user has successfully passed the signup step before verifying an OTP.

### [src/app/api/auth/signup/route.ts](file:///e:/project/sudaisai/src/app/api/auth/signup/route.ts)
- **Updates:** Replaced duplicate logic with shared pi-helpers. Generates and returns a JWT token. Removed rate limiting logic.
- **Updates:** Replaced duplicate logic with shared  pi-helpers. Generates and returns a JWT token. Removed rate limiting logic.

### [src/app/api/auth/verify-otp/route.ts](file:///e:/project/sudaisai/src/app/api/auth/verify-otp/route.ts)
- **Updates:** Completely restructured to validate the JWT token instead of raw user inputs. Removed rate limiting and attempt tracking logic.

### [prisma/schema.prisma](file:///e:/project/sudaisai/prisma/schema.prisma)
- **Updates:** Removed the  ttempts field from PendingRegistration as attempt locking was removed.

---

## 📌 8. Authentication UI Pages (`src/app/auth/`)

### 8.1 [src/app/auth/page.tsx](file:///e:/project/sudaisai/src/app/auth/page.tsx)
- 🎯 **Why it Exists:** Provides the user-facing interface for user authentication.
- 📄 **What it Contains:** A unified page featuring both "Log In" and "Sign Up" tabs, social authentication buttons (Google, Apple), and email/password forms with visibility toggling.
- ⚙️ **Function & Purpose:** Serves as the primary gateway for users entering the platform.
- 🔄 **How it Works:** Client-side React component maintaining local state for the active tab (`isLogin`) and password visibility.

### 8.2 [src/app/auth/reset/page.tsx](file:///e:/project/sudaisai/src/app/auth/reset/page.tsx)
- 🎯 **Why it Exists:** Allows users who forgot their passwords to request a password reset link.
- 📄 **What it Contains:** A minimalistic form requesting the user's email address, and a submission confirmation screen.
- ⚙️ **Function & Purpose:** Initiates the account recovery process.
- 🔄 **How it Works:** Client-side React component linked from the main auth page's "Forgot Password?" link.

### 8.3 [src/app/auth/verify-otp/page.tsx](file:///e:/project/sudaisai/src/app/auth/verify-otp/page.tsx)
- 🎯 **Why it Exists:** Provides an interface for entering the 6-digit One-Time Password sent to the user during registration.
- 📄 **What it Contains:** A 6-input layout for entering the verification code, supporting auto-advance, backspace handling, and paste events.
- ⚙️ **Function & Purpose:** Captures the OTP required to finalize user registration and verify email ownership.
- 🔄 **How it Works:** Client-side React component that manages an array of 6 digit states and auto-focuses input refs dynamically.
